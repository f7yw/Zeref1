providers = ["node"]

[variables]
NIXPACKS_NODE_VERSION = "20"

[phases.setup]
nixPkgs = ["nodejs_20","python3", "ffmpeg","pnpm-9_x","libvips","gcc", "pkg-config"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile --prod=false","pip3 install yt-dlp"]

[start]
cmd = "node index.js"
