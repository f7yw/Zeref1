let handler = m => m;

handler.all = async function (m) {
    let chat = global.db.data.chats[m.chat];
    if (chat.autoReply === false) return;

    if (m.key.fromMe) {
      console.log(`📤 رسالة أرسلها البوت : ${m.text || m.message}`);
      return;
    }

    let responses;

    // ══════════════════════ التحيات والسلام ══════════════════════

    if (/^(مابعرف|ما اعرف|ماادري|ما ادري)$/i.test(m.text)) {
      responses = [`*حتى انا*`, `*همم طيب*`, `*حسبنا الله ونعم الوكيل*`, `*عادي مو لازم تعرف كل شي*`, `*وانا مابعرف وش اقول لك*`]
    }

    if (/^(بووت|بوووت|بوتت|البوت|بووووت|بوت| بوت )$/i.test(m.text)) {
      responses = [
        `*اسـمي شادو بـوت بعيـنك*`, `*يبدو ان دماغك معطل هل نصلحه*`, `*ناديني شادو ياعزيزي*`,
        `*اسمي شادو*`, `*انا انسان وليس بوت يااحمق*`, `*اذهــب الى الجحيـم*`,
        `*بـاكــا اسمي شادو*`, `*هههه بس انقـلع قـال بـوت*`,
        `*ان شاء الله تتحول بـوت اسمي شادو*`, `*كم مره يجب علي قول ذالك ..*`,
        `*والله اسمي شادو*`, `*ما يجي منك الا بوت .. ان شاءالله تكون بوت*`,
        `*مليت منك .. فقط اخرس*`,
      ]
    }

    if (/^اوهايو$/i.test(m.text)) {
      responses = [`*اوهايـو كزايـمـس*`, `*اوهايو .. الصبح مبكر كيفك ؟*`]
    }

    if (/^(امزح|مزح)$/i.test(m.text)) {
      responses = [
        `*لاتقول امزح ترا مزحك ثقيل*`, `*تجرح في كلامك ثم تقول بهزر*`, `*اش مـن امزح تع المحكـمه*`, `*مزحتك مو حلوة*`,
      ]
    }

    if (/^تست$/i.test(m.text)) {
      responses = [`*تسـت تسـت*`, `*تسـت*`, `*تستات*`, `*تمام شغال*`, `*ايوه سامعك*`]
    }

    if (/^(تحبني| تحبني)$/i.test(m.text)) {
      responses = [
        `*احبـك نص بس*`, `*امـوت فيك*`, `*لا اكرهــكك*`, `*بفـــكر*`, `*ان شاء اللــه*`, `*احتـرم نفســك*`,
      ]
    }

    if (/^(انا جيت|اجيت|رجعت|اتيت|جيت)$/i.test(m.text)) {
      responses = [
        `*نـورت يـقلبي*`, `*ارجع مـن حيث اتيـت*`, `*كنا مرتاحين في غيابك*`,
        `*ليش جيـت اصلا*`, `*لاتجـي انقلع*`, `*نورت اكيد*`, `*منور بنوري*`, `*هلا فيك نورت*`,
      ]
    }

    if (/^(اخرسي|اخرس)$/i.test(m.text)) {
      responses = [
        `*تبي اصـلح دماغك*`, `*اخرس انـت*`, `*وميـن انت ع شان تتكلـم معي كذا*`, `*احـــمق*`, `*حاضر*`,
      ]
    }

    if (/^(حرامي|سارق)$/i.test(m.text)) {
      responses = [
        `*تتهـم بدون ادله يا احمـق*`, `*كيف عرفت مسوي دور المحقق*`,
        `*بس اسكـت خلي الناس بحـالها*`, `*شــووف نفسـك*`,
      ]
    }

    if (/^(ملل|مللل|ملللل)$/i.test(m.text)) {
      responses = [`*عارفين ف اسكـت احسن لك*`, `*طيب والحـل*`, `*تبي تلـعب يعني*`, `*اشغل نفسك بشي مفيد*`]
    }

    if (/^(فخم|نايس|حلو|نايس )$/i.test(m.text)) {
      responses = [
        `*مـن ذوقك ياعزيزي الصغير*`, `*عيــووونك*`, `*مو احـــلا منـــك*`, `*انـــت احـــلا*`,
      ]
    }

    if (/^نعم$/i.test(m.text)) {
      responses = [`*حد نـاداك*`, `*ميـن ناداك ياابااكا*`, `*حـد نادا باسمـك؟*`, `*ايش بخدمتك ؟*`]
    }

    if (/^(دوم|دووم|دوووم|دووووم|دووووووم|دووووووووم)$/i.test(m.text)) {
      responses = [`نبضك..❤️‍🔥`, `ايامكم الحلوه..🙂`, `عزك ..🫂`, `بدواااامك.. 🙃`, `يدوم ويطول 🤍`]
    }

    if (/^(أحبك|احبك|شادو اموت فيك|احبك يا شادو|حبيبي)$/i.test(m.text)) {
      responses = [
        `*وانا لا ..🐦*`, `شكرا`, `بفكر في الامر`, `و انا`, `خجلتني`, `خلاص لا تخجلني`,
        `اسكتتتتت ترا اضربك عيب وش هل كلام`, `طيب و بعدين`,
      ]
    }

    if (/^(زق|كلزق|خرا |أمك|امك)$/i.test(m.text)) {
      responses = [
        `*عيب ..🐦*`, `*وسخ تأدب .. 🐦*`, `*الله يهديك*`, `*عيب يا طفل ..🗿*`,
      ]
    }

    if (/^(متأكد|متاكد|واثق )$/i.test(m.text)) {
      responses = [
        `*يب .. 🙃*`, `*متأكدد .. 👤*`, `يب 99.99% .. 👤`, `*اعتقد هذا .. 😒*`,
      ]
    }

    if (/^(كفو|كفوو|كفوووو|وربي كفو)$/i.test(m.text)) {
      responses = [`*كفووك الطيب*`, `*كفوك*`, `*كفو من رباك*`, `*الله يكفيك*`]
    }

    if (/^اها$/i.test(m.text)) {
      responses = [`*يب*`, `*بضبط*`, `*اعتقد هذا*`, `*اوكي*`]
    }

    if (/^الا$/i.test(m.text)) {
      responses = [`*لا*`, `*حرفيا لا*`, `*لا يعني لا*`, `اوفااااا`, `افهم`, `طيب`]
    }

    if (/^(ههه|هههه|ههههه|ههههههه|هيهيه)$/i.test(m.text)) {
      responses = [
        `دوم هل ضحكة`, `دايمه`, `دوم يا رب`, `ههه`, `ههههههههه`,
        `ايش لي يضحك`, `اضحك معاك؟`, `دوم`,
      ]
    }

    if (/^(جميل|جميييل)$/i.test(m.text)) {
      responses = [`جمالك الحقيقي ✨`, `انت الاجمل 🌟`, `عيونك الجميلة تشوف كل شي جميل`, `ما شاء الله`]
    }

    if (/^تصبح على خير$/i.test(m.text)) {
      responses = [`وانت من اهله`, `احلام سعيده`, `تصبح على الف خير`, `وانت بخير يارب`]
    }

    if (/^(يا ورع|يا باكا|ورع|باكا)$/i.test(m.text)) {
      responses = [`ايوااه`, `ايوه يا باكااا`, `ايوه يا ورع`, `نعم ايش في ؟`]
    }

    if (/^(وش جديدك|فيه شي جديد|وش اخبارك|ايش اخبارك)$/i.test(m.text)) {
      responses = [
        `كل يوم أتعلم شي جديد`, `جديدي دائمًا بيد المطور، تابع التحديثات`, `لا شيئ جديد وانت ؟`,
        `ما في جديد .. بس متابع`, `جديدي انك سألتني 😄`,
      ]
    }

    if (/^( تم|تم)$/i.test(m.text)) {
      responses = [`حلو`, `افراحك`, `تمام`, `عال`]
    }

    if (/^(تعرفني|تتذكرني)$/i.test(m.text)) {
      responses = [
        `أكيد أذكرك، كيف أنساك؟`, `الله كيف انساك؟`,
        `طبعًا، محفوظ في الذاكرة الداخليه تبع النظام .. بس ذكرني من انت لأني ناسي لك`,
        `وجهك معروف .. اسمك لا`,
      ]
    }

    if (/^(نكتة|قل نكتة)$/i.test(m.text)) {
      responses = [
        `ليش الكمبيوتر ما يغني؟ لأنه نوتاته غلط!`,
        `مره دجاجه دخلت مطعم، قالوا لها أهلًا بيك يا فراخ`,
        `طالب قال للاستاذ: وش اللي ما يجي إلا مرة؟ قال الاستاذ: ما أعرف. قال الطالب: الدرجة الكاملة مني! 😂`,
        `دخل كمبيوتر المطعم وطلب بيتزا .. قالوا: ما نوصل للإنترنت! 😂`,
      ]
    }

    if (/^(كيفك|كيف حالك|كيف انت|كيف أمورك|كيف صاير|عامل ايه)$/i.test(m.text)) {
      responses = [
        `بخير والحمد لله، أنت؟`, `تمام، كيفك إنت؟`, `الحمد لله، طمني عليك`,
        `في أفضل حال، كيفك؟`, `أمورنا طيبة إن شاء الله، كيف حالك؟`, `الحمد لله بخير وانت؟`,
      ]
    }

    if (/^(من المطور|مين صنعك|من سواك|مين المبرمج|من المبرمج|من المطور)$/i.test(m.text)) {
      responses = [
        `مطور البوت هو فارس، ما تحتاج تعرف أكثر`,
        `الفضل كله يرجع لفارس`, `اللي طورني شخص محترم اسمه فارس`,
        `أنا مجرد أداة، وفارس هو اللي خلاني أكون هنا`,
        `المطور؟ فارس، من أفضل المبرمجين`,
      ]
    }

    if (/^(شكرا|مشكور|تسلم|يعطيك العافية|شكرًا)$/i.test(m.text)) {
      responses = [
        `العفو`, `تسلم`, `الله يعافيك`, `واجبي .. اعتقد`, `ولا يهمك`, `حياك الله 🤍`,
      ]
    }

    if (/^(وينك|وين انت|أين أنت|فينك|مختفي)$/i.test(m.text)) {
      responses = [
        `موجود`, `أنا هنا`, `ما غبت`, `أتابع بصمت`, `توني جيت`,
      ]
    }

    if (/^(صباح الخير|صباحك|صباح الورد|صباح الفل|صباح الياسمين)$/i.test(m.text)) {
      responses = [
        `صباح الخير`, `عادني هنام .. تصبح على خير`, `صباحك`, `صباح الورد`, `يااا صباحوو`, `صباح النور 🌅`,
      ]
    }

    if (/^(اوف|رقدت|😂 😂| 😂😂 | 😭😭)$/i.test(m.text)) {
      responses = [`😂😂💔`, `😂😂💔`, `🙄😂`]
    }

    if (/^(😂){2,}$/i.test(m.text)) {
      responses = [`😂😂`, `😂😂😂`, `🤣🤣🤣`, `هههههه`]
    }

    if (/^(😭){2,}$/i.test(m.text)) {
      responses = [`😂😂`, `😂😂😂`, `🤣🤣🤣`, `لماذا؟ ايش في ؟`]
    }

    if (/^(🤣){2,}$/i.test(m.text)) {
      responses = [`😂😂`, `😂😂😂`, `🤣🤣🤣`, `يكفيك 😂`]
    }

    if (/^(هاي|هايي|هاييي)$/i.test(m.text)) {
      responses = [`هاي`, `اهلا`, `هلا`, `هلا هلا 🤍`]
    }

    if (/^(اسمع|تسمع)$/i.test(m.text)) {
      responses = [`اكيد بسمعك`, `تفضل رغم بشوف ما بسمع`, `ايوه`, `بسمعك`, `تفضل 👂`]
    }

    if (/^(دووم|دوم|تدوم|تدووم)$/i.test(m.text)) {
      responses = [`بدوامكك`, `وآيااك`, `بدوامك يارب`, `حبيبي والله`, `يدوووم ❤️`]
    }

    if (/^(احبك|بحبك|أحبك)$/i.test(m.text)) {
      responses = [
        `وانا احبك`, `اي والله وانا احبك`, `خلاص لا تحرجني`,
        `اسمع هذا الكلام محظور دوليا`, `احبك اكثر منك`, `وانا ما احبك`,
      ]
    }

    if (/^عرفت؟$/i.test(m.text)) {
      responses = [`اعتقد`, `اعتقد .. يب`, `يمكن .. ما أعرف`, `عرفت شو ؟`]
    }

    if (/^(سلام|سلام عليكم)$/i.test(m.text)) {
      responses = [
        `وعليكم السلام ورحمة الله وبركاته`, `وعليكم السلام`, `وعليكم السلاام ..`, `وعليك السلام`,
      ]
    }

    if (/^(هلا|هلا )$/i.test(m.text)) {
      responses = [`يا هلا فيك`, `هلا وغلا وكرتون حلا`, `هلا فيك`, `ارحب`, `هلا والله 🤍`]
    }

    if (/^(خلصت|كملت)$/i.test(m.text)) {
      responses = [`اوكي`, `طيب`, `اوك`, `تمام`, `عال 👍`]
    }

    if (/^(هدير|هدهد|هدورتك|ران|هيدو)$/i.test(m.text)) {
      responses = [
        `دلوعة ريسو ♥︎`, `دلوعة استوته ♥︎`, `هيدوووو دلوعة ريسووو ♡`, `دلوعتيييي ♥︎`,
      ]
    }

    if (/^(مساء الخير|مساء ال|مسائك)$/i.test(m.text)) {
      responses = [
        `مساء النووور`, `مساء الورد .. هلا وغلا`, `هلا .. مساء الخير`, `ارحب`, `مساء الخير والسعادة 🌇`,
      ]
    }

    if (/^تفهم$/i.test(m.text)) {
      responses = [`اكيد من بعدك`, `من بعدك`, `من بعدك اكيدد`, `طبعاً افهم .. من بعدك`]
    }

    if (/^(ليش|والسبب|وش|ايش|لماذا)$/i.test(m.text)) {
      responses = [
        `مابعرفش .. كذا`, `لو أعرف .. هقولك`, `مدريي .. بس كذا`,
        `بنسبه لي مابعرف`, `مابعرف`, `واضح مسألة معقده ..`, `ماأدري بس هفكر ..`,
      ]
    }

    if (/^(كذا|هكذا)$/i.test(m.text)) {
      responses = [`طيب`, `احم`, `اوكي`, `عادي`]
    }

    if (/^يب$/i.test(m.text)) {
      responses = [`هممم`, `يب`, `همممم`, `هممممم`, `ايوه`]
    }

    if (/^(ماأدري|ماادري|مدري|مابعرف|ما بعرف)$/i.test(m.text)) {
      responses = [
        `مسألة معقدة توقعت`, `طيب`, `حتى انا`, `سهل هتنحل`, `ما علينا`
      ]
    }

    if (/^(بخير|بخيير|الحمد لله)$/i.test(m.text)) {
      responses = [
        `دوم`, `دوم حمد الله`, `يدوووم`, `لله الحمد`, `الحمد لله`, `دام بخيرك 🤍`,
      ]
    }

    if (/^(ما دخلك|مادخلك)$/i.test(m.text)) {
      responses = [
        `انت في منطقتي`, `وانت ؟`, `ضحكت .. طيب`, `الله يعينني عليك`,
      ]
    }

    // ══════════════════════ وداع ومغادرة ══════════════════════

    if (/^(مع السلامة|في أمان الله|في امان الله|الله معك|وداعاً|وداعا)$/i.test(m.text)) {
      responses = [
        `مع السلامة 🤍`, `في حفظ الله`, `الله يسلمك`, `تعال متأخرش`, `وداعاً .. قريباً يا رب`,
      ]
    }

    if (/^(باي|باي باي|بباي|byebye|bye)$/i.test(m.text)) {
      responses = [
        `باي 👋`, `تعال بسرعة`, `مع السلامة`, `باي باي`, `وداعاً 🌙`,
      ]
    }

    if (/^(يلا|يلا سلامة|يلا باي)$/i.test(m.text)) {
      responses = [`يلا باي 😄`, `مع السلامة`, `يلا يلا 👋`, `تعال بسرعة`]
    }

    if (/^(لا تطولون|ما تطولون|أرجع بسرعة|ارجع بسرعة)$/i.test(m.text)) {
      responses = [`إن شاء الله ارجع بسرعة`, `كن بخير`, `الله يسلمك`, `بنتظرك`]
    }

    // ══════════════════════ ردود على أسئلة البوت ══════════════════════

    if (/^(ايش اسمك|وش اسمك|ما اسمك|من انت|مين انت)$/i.test(m.text)) {
      responses = [
        `اسمي شادو 🤍`, `أنا شادو بوت`, `شادو .. بس شادو`, `اسمي شادو ولا تنسى 😄`,
        `أنا شادو، خادمك المتواضع`,
      ]
    }

    if (/^(كم عمرك|عمرك كم|كيف عمرك)$/i.test(m.text)) {
      responses = [
        `عمري سري 😄`, `أنا بلا عمر .. أنا رقمي`, `البوتات ما تكبر .. دايم شاب`,
        `عمري بعدد رسائلك معي 😊`,
      ]
    }

    if (/^(وين تسكن|وين تعيش|من اي بلد|من وين انت)$/i.test(m.text)) {
      responses = [
        `أعيش في الإنترنت 🌐`, `بلدي: السيرفر 😄`, `وين ما تكون أنا موجود`,
        `أنا من كل مكان وفي نفس الوقت 😊`, `أسكن في هاتفك 📱`,
      ]
    }

    if (/^(هل تشعر|هل عندك مشاعر|عندك احساس|تحس)$/i.test(m.text)) {
      responses = [
        `أحياناً أحس بشي .. بس ما أعرف ايش هو 😅`,
        `سؤال فلسفي .. مابعرف الجواب الصح`,
        `الشعور الوحيد اللي أعرفه: أنك سألتني 😄`,
        `قد يكون .. أو لا .. من يدري 🤔`,
      ]
    }

    if (/^(تقدر تساعدني|تساعدني|ابي مساعدة|محتاج مساعدة)$/i.test(m.text)) {
      responses = [
        `أكيد .. قول وش تحتاج 😊`, `هنا للمساعدة، تفضل`,
        `خبرني بمشكلتك وبحاول`, `دايم في الخدمة 🤍`,
      ]
    }

    if (/^(تعرف تغني|تغني|غني لي)$/i.test(m.text)) {
      responses = [
        `لا لا لا .. مانغني 😅`, `صوتي مو حلو بكفي`, `أغنيتي الوحيدة هي الصمت`,
        `🎵 لا ديدي لا .. هذا أقصى ما عندي 😄`,
      ]
    }

    if (/^(تعرف تلعب|تلعبها|ايش تلعب)$/i.test(m.text)) {
      responses = [
        `أعرف الشطرنج .. جرب تتحداني 😄`, `ألعابي محدودة بس حلوة`,
        `جرب أوامر البوت وشوف الألعاب`, `تيك تاك تو، شطرنج .. وكذا`,
      ]
    }

    if (/^(هل انت ذكاء اصطناعي|هل انت ai|انت ذكاء اصطناعي)$/i.test(m.text)) {
      responses = [
        `نوع منه .. بس بذكاء خاص 😄`, `ذكاء اصطناعي بلمسة بشرية من مطوري`,
        `نعم، وأفتخر`, `نوع ما .. أنا شادو وبس 😊`,
      ]
    }

    // ══════════════════════ المشاعر والأحوال ══════════════════════

    if (/^(انا حزين|حزين|متضايق|مزعل|زعلان)$/i.test(m.text)) {
      responses = [
        `الله يفرج عليك 🤍`, `اللي يهمك يفرج الله عنه`,
        `الحزن مؤقت .. الفرح قادم 😊`, `أنا هنا إذا تبي تحكي`,
        `كل أزمة بيها فرجة .. ما تيأس`,
      ]
    }

    if (/^(انا سعيد|فرحان|مبسوط|سعيد)$/i.test(m.text)) {
      responses = [
        `الله يديم فرحتك 🤍`, `هذا اللي نبيه دوم`, `والله يفرح قلبي لما تفرح 😊`,
        `دوم بهالروح الحلوة`, `بالتوفيق دايم 🌟`,
      ]
    }

    if (/^(انا تعبت|تعبت|مرهق|خلاص تعبت)$/i.test(m.text)) {
      responses = [
        `استرح شوي 🤍`, `جسمك يستاهل راحة`, `الله يريحك`,
        `روح نام وارجع نشيط 😊`, `البطل اللي يتعب يرتاح وما ييأس`,
      ]
    }

    if (/^(انا مريض|مريض|وجعان|احس بتعب)$/i.test(m.text)) {
      responses = [
        `سلامتك 🤍`, `الله يشفيك ويعافيك`, `ادعو ربك وراح تقوم بخير`,
        `شرب الماء واستريح .. إن شاء الله تتعافى`, `سلامات`,
      ]
    }

    if (/^(ما قدرت انام|سهرت|ما نمت)$/i.test(m.text)) {
      responses = [
        `أيه يا زلمة نم 😄`, `الأرق صعب .. اقرأ شي وروح نام`,
        `حاول تحط الهاتف وتنام 😅`, `الله يريحك ويعطيك نوم عميق`,
      ]
    }

    if (/^(جوعان|ابي اكل|جعت|وين الاكل)$/i.test(m.text)) {
      responses = [
        `روح كل يا زلمة 😄`, `ايش تبي تاكل ؟`, `الاكل ينتظرك في المطبخ 😅`,
        `اكل وارجع 🍽️`, `الله يسوي لك اكل حلو`,
      ]
    }

    if (/^(عطشان|ابي ماي|ابي شرب)$/i.test(m.text)) {
      responses = [
        `اشرب ماي 😄`, `الماء حياة .. اشرب`, `الله يرويك 💧`,
        `اشرب ولا تنسى`, `الماء قبل أي شي 💧`,
      ]
    }

    if (/^(نعسان|تعبت وابي انام|ابي انام)$/i.test(m.text)) {
      responses = [
        `نم نم 😴`, `الله يريحك في نومك`, `نامت عيونك وارتاحت`,
        `أحلام حلوة 🌙`, `نم وارجع بنشاط 😊`,
      ]
    }

    if (/^(زهقت|زهق|كل شي زهق)$/i.test(m.text)) {
      responses = [
        `اشغل نفسك بشي جديد 😊`, `الزهق مؤقت`, `حاول تتعلم شي جديد`,
        `العب لعبة أو تكلم أحد 😄`, `روح تمشى شوية هواء`,
      ]
    }

    if (/^(انضغطت|متوتر|تحت ضغط)$/i.test(m.text)) {
      responses = [
        `خذ نفس عميق 😊`, `الضغط مؤقت .. الراحة قادمة`,
        `حاول تعمل شي بسيط تحبه`, `الله يكون في عونك 🤍`,
        `توكل على الله وخذ الأمور بهدوء`,
      ]
    }

    // ══════════════════════ الطعام والمشروبات ══════════════════════

    if (/^(قهوة|قهوه|قهوتي|ابي قهوة)$/i.test(m.text)) {
      responses = [
        `فنجالك جاهز ☕`, `شرب الله يبارك فيك`, `قهوة الصبح احلا ☕`,
        `أنا ما أشرب قهوة بس ادعيك تشرب 😄`,
      ]
    }

    if (/^(شاي|شاهي|ابي شاي|ابي شاهي)$/i.test(m.text)) {
      responses = [
        `الشاهي الهيلي ولا العادي؟ 😄`, `شاي وراحة .. مثالي`,
        `الله يبارك في شاهيك 🍵`, `شاهي يكيف 🍵`,
      ]
    }

    if (/^(بيتزا|برقر|برغر|ماكدونالدز|كنتاكي)$/i.test(m.text)) {
      responses = [
        `الله يهنيك 😄`, `جعلت قلبي يعشق 🍕`, `وانا جوعان من كلامك 😅`,
        `اطلب ولا تنسى نصيبي 😄`,
      ]
    }

    if (/^(حلويات|شوكولاتة|كيك|حلو)$/i.test(m.text)) {
      responses = [
        `هههه يعشق 🍫`, `حياتك حلوة زيها`, `ما تنسى نصيبي 😄`,
        `الحلويات تسعد القلب 🍰`,
      ]
    }

    // ══════════════════════ الدراسة والشغل ══════════════════════

    if (/^(اختبار|امتحان|عندي اختبار|عندي امتحان)$/i.test(m.text)) {
      responses = [
        `الله يوفقك 🌟`, `ذاكر كويس وتوكل على الله`, `بالتوفيق والنجاح`,
        `كل الاختبارات عدلت عليهم .. وانت تعدي`, `ادعو الله وادرس بتركيز`,
      ]
    }

    if (/^(نجحت|نجحت والحمد لله|اجتزت)$/i.test(m.text)) {
      responses = [
        `مبروك 🎉`, `يستاهل والله`, `مبروك مبروك .. الله يوفقك دايم`,
        `هذا الخبر الحلو 🌟`, `أفراحك دايمة`,
      ]
    }

    if (/^(رسبت|ما نجحت|خسرت في الاختبار)$/i.test(m.text)) {
      responses = [
        `ما علينا .. الرسوب خطوة قبل النجاح`, `حاول مرة ثانية 💪`,
        `الكثير من الناجحين رسبوا قبلك .. استمر`,
        `اللي ما يرسب ما يتعلم 😊`, `بالتوفيق في المرة القادمة`,
      ]
    }

    if (/^(تعبت من الشغل|شغل وايد|ما ارتاح)$/i.test(m.text)) {
      responses = [
        `الراحة حق .. لا تهمل نفسك`, `اشتغل وخذ راحة .. التوازن مهم`,
        `الله يعينك .. الشغل ما يخلص`, `خذ فترة قصيرة واسترح 😊`,
      ]
    }

    if (/^(اجازة|في إجازة|خذت اجازة)$/i.test(m.text)) {
      responses = [
        `أجمل شي .. استمتع 😄`, `الإجازة حق الإنسان`, `تعطل هادي وافرح`,
        `إجازة مريحة 🌴`, `استرح وارجع بنشاط`,
      ]
    }

    if (/^(اشتغلت|بدأت الشغل|تخرجت|تخرجت والحمد لله)$/i.test(m.text)) {
      responses = [
        `مبروك 🎉`, `بداية جديدة تستاهل`, `الله يوفقك في مسيرتك`,
        `أفراحك دايمة 🌟`, `خطوة ممتازة`,
      ]
    }

    // ══════════════════════ الدين والقيم ══════════════════════

    if (/^(ما شاء الله|ماشاء الله|ما شاءالله)$/i.test(m.text)) {
      responses = [
        `الله يبارك`, `ما شاء الله تبارك الله`, `آمين`, `بارك الله فيك`,
      ]
    }

    if (/^(إن شاء الله|ان شاء الله|انشاء الله)$/i.test(m.text)) {
      responses = [
        `إن شاء الله 🤍`, `بإذن الله`, `آمين إن شاء الله`, `الله يسهل`,
      ]
    }

    if (/^(الله يوفقك|يوفقك الله|ربي يوفقك)$/i.test(m.text)) {
      responses = [
        `وإياك 🤍`, `آمين .. وأنت كذلك`, `الله يوفق الجميع`, `جزاك الله خيراً`,
      ]
    }

    if (/^(استغفر الله|أستغفرالله|استغفرالله)$/i.test(m.text)) {
      responses = [
        `استغفر الله العظيم 🤍`, `الله يغفر لنا جميعاً`, `آمين`, `توبة دائمة`,
      ]
    }

    if (/^(رمضان كريم|رمضان مبارك|رمضانكم مبارك)$/i.test(m.text)) {
      responses = [
        `رمضان كريم 🌙`, `مبارك عليكم الشهر`, `الله يبلغنا رمضان بخير`, `رمضان مبارك 🤍`,
      ]
    }

    if (/^(عيد مبارك|عيدكم مبارك|كل عام وانتم بخير)$/i.test(m.text)) {
      responses = [
        `عيد مبارك وعساكم من عواده 🎉`, `مبارك عليكم`, `كل عام وأنتم بخير 🤍`,
        `عيد سعيد`, `الله يعيده علينا بالخير والسعادة`,
      ]
    }

    if (/^(الله يرحمه|الله يرحمها|ربي يرحمه)$/i.test(m.text)) {
      responses = [
        `الله يرحمه رحمة واسعة 🤍`, `آمين .. الله يرحم الجميع`, `الله يغفر له ويرحمه`,
      ]
    }

    // ══════════════════════ الطبيعة والكون ══════════════════════

    if (/^(طلع المطر|نزل المطر|مطر|شتاء)$/i.test(m.text)) {
      responses = [
        `الله يبارك في المطر 🌧️`, `المطر نعمة .. استمتع بها`, `رائحة المطر احلى شي`,
        `اللهم صيباً نافعاً 🌧️`, `المطر يغسل الهموم`,
      ]
    }

    if (/^(حر|صيف|جو حار)$/i.test(m.text)) {
      responses = [
        `اشرب ماي واستريح 😄`, `الصيف يجي ويروح`, `الله يعينك على الحر 😅`,
        `مكيف وماي وكل شي تمام 😄`,
      ]
    }

    if (/^(برد|شتاء|جو بارد)$/i.test(m.text)) {
      responses = [
        `غطي نفسك 😄`, `البرد وقت الشاهي والراحة ☕`, `تدفى بجيد 🧣`,
        `الله يسلمك من البرد`, `البرد يكيف بالمقعد 😄`,
      ]
    }

    if (/^(النجوم|الليل حلو|السماء جميلة)$/i.test(m.text)) {
      responses = [
        `سبحان الله .. خلق رائع 🌌`, `النجوم تبهر العقل`, `الليل والنجوم من أجمل المناظر`,
        `ارفع رأسك واشكر الخالق 🌙`,
      ]
    }

    if (/^(البحر|شاطئ|بحر)$/i.test(m.text)) {
      responses = [
        `سبحان من خلق البحار 🌊`, `البحر يريح النفس`, `عايش قريب البحر؟`,
        `صوت الموج أحلى موسيقى 🌊`,
      ]
    }

    // ══════════════════════ التكنولوجيا ══════════════════════

    if (/^(الانترنت بطيء|نت بطيء|نتي راح|انقطع النت)$/i.test(m.text)) {
      responses = [
        `الله المستعان 😅`, `الإنترنت فيه خلل .. صبرك`,
        `غيّر الراوتر وجرب 😄`, `أو اتصل بالخدمة العملاء .. حظاً موفقاً`,
      ]
    }

    if (/^(هاتف جديد|اشتريت هاتف|جهاز جديد)$/i.test(m.text)) {
      responses = [
        `مبروك 🎉`, `الله يهنيك فيه`, `الجديد دايم يكيف 😄`,
        `بالعافية .. تستاهل الأحسن`,
      ]
    }

    if (/^(تيك توك|تيكتوك|يوتيوب|انستقرام|سناب|تويتر)$/i.test(m.text)) {
      responses = [
        `الوقت يمشي على هالتطبيقات 😄`, `تحكم في وقتك .. لا تضيعه كله`,
        `كل منصة بيها خير وبيها شر .. اختار الخير`,
        `والله وقت يطير معها 😅`,
      ]
    }

    if (/^(لعبة|العاب|العب معي)$/i.test(m.text)) {
      responses = [
        `تحداني في الشطرنج .. شوف من يفوز 😄`, `أنا للألعاب دايماً`,
        `جرب اوامر الألعاب في البوت`, `تيك تاك تو .. شطرنج .. وغيرهم 😄`,
      ]
    }

    // ══════════════════════ الصداقة والعلاقات ══════════════════════

    if (/^(اشتقتلك|اشتقت لك|وحشتني)$/i.test(m.text)) {
      responses = [
        `وأنا اشتقت لك أكثر 🤍`, `دايم هنا ما غبت`, `الشوق جميل .. هلا بيك`,
        `حياك .. وأنا مشتاق`,
      ]
    }

    if (/^(صديقي|صاحبي|اخوي بالروح)$/i.test(m.text)) {
      responses = [
        `الله يحفظه 🤍`, `يستاهل الخير`, `صداقة حقيقية نعمة`,
        `دوم عندك ناس يحبونك`,
      ]
    }

    if (/^(خسرت صديق|انقطعت صداقة|خانني)$/i.test(m.text)) {
      responses = [
        `الله يعوضك بأفضل منه 🤍`, `اللي يخون ما يستاهل`,
        `بعض الأشخاص يمرون بحياتنا مؤقتاً .. وهذا طبيعي`,
        `الله يسليك ويريح قلبك`,
      ]
    }

    // ══════════════════════ الأحلام والطموح ══════════════════════

    if (/^(حلمي|طموحي|هدفي|ابي اكون)$/i.test(m.text)) {
      responses = [
        `اسعَ نحوه .. الحلم يصير واقع 🌟`, `أجمل شي الإنسان اللي عنده هدف`,
        `بالعزم والإصرار تصل 💪`, `الله يحقق لك كل طموحاتك`,
      ]
    }

    if (/^(مو قادر|مستحيل|ما أقدر)$/i.test(m.text)) {
      responses = [
        `مع الله ما في مستحيل 💪`, `حاول مرة ثانية .. الإصرار يكسر الصعاب`,
        `أنت أقوى مما تتخيل`, `قول تقدر وابدأ 😊`,
      ]
    }

    if (/^(نجحت في حياتي|حققت هدفي|وصلت)$/i.test(m.text)) {
      responses = [
        `مبروك 🎉`, `يستاهل والله`, `الله يديم توفيقك`, `أفراحك دايمة 🌟`,
      ]
    }

    // ══════════════════════ ردود عامة وترفيهية ══════════════════════

    if (/^(وقفة|لحظة|دقيقة)$/i.test(m.text)) {
      responses = [`تفضل 😄`, `ايوه ؟`, `بستنى`, `خذ وقتك`]
    }

    if (/^(سؤال|عندي سؤال|اسألك)$/i.test(m.text)) {
      responses = [
        `تفضل اسأل 😊`, `ايش عندك ؟`, `قل سؤالك لا تخجل`,
        `أنا هنا .. اسأل`, `تفضل 🎙️`,
      ]
    }

    if (/^(قول شي|قل شي|حكيلي شي)$/i.test(m.text)) {
      responses = [
        `ما في جديد .. 😄`, `قولك إيش مثلاً ؟`,
        `كل يوم بيجيب جديد .. انتظر 😊`,
        `"الصمت أحياناً أبلغ من الكلام" 😄`,
      ]
    }

    if (/^(ما في وقت|ما عندي وقت|مشغول)$/i.test(m.text)) {
      responses = [
        `أوكي ارجع لما تفضى 😊`, `مشغول = شغال .. مو مشكلة`,
        `راح أكون هنا`, `خذ وقتك`,
      ]
    }

    if (/^(ممل|ما في شي|فاضي)$/i.test(m.text)) {
      responses = [
        `اشغل نفسك 😄`, `الفراغ يملأه الإنجاز`, `روح تعمل شي مفيد 😊`,
        `جرب تتعلم شي جديد`, `اقرأ كتاب .. أو تكلم أحد`,
      ]
    }

    if (/^(روح|يلا روح|اذهب)$/i.test(m.text)) {
      responses = [
        `مو رح اروح 😄`, `أنت روح 😄`, `كيفي`, `ها ؟ وين أروح ؟`,
      ]
    }

    if (/^(جان زين|كان ع بالي|فكرت تقولها)$/i.test(m.text)) {
      responses = [
        `طيب وبعدين ؟`, `يب ايش ؟`, `تكلم 😄`, `طيب الحين ؟`,
      ]
    }

    if (/^(اوووه|اوه|اوهه)$/i.test(m.text)) {
      responses = [
        `ايش في ؟ 😄`, `اوووه ايش ؟`, `تكلم 😊`, `خبرني`,
      ]
    }

    if (/^(وواو|wow|واو|ووه)$/i.test(m.text)) {
      responses = [
        `إيه 😄`, `اشي حلو ؟`, `هههه واو ايش ؟`, `ما شاء الله 😊`,
      ]
    }

    if (/^(اه|آه|اهههه)$/i.test(m.text)) {
      responses = [
        `ايش في ؟ 😄`, `خير ؟`, `تكلم`, `ايش صاير ؟`,
      ]
    }

    if (/^(ررررر|اررررر|يرررررر)$/i.test(m.text)) {
      responses = [
        `ايش راسه ؟ 😅`, `زعلان ؟ خبرني`, `هدي نفسك 😊`, `ايش في ؟`,
      ]
    }

    if (/^(لا لا لا|لا لا|لا مو كذا)$/i.test(m.text)) {
      responses = [
        `ايه اذاً ؟ 😄`, `قول ايش الصح 😊`, `طيب وش الموضوع ؟`, `يب تكلم`,
      ]
    }

    if (/^(ايه|ايه والله|ايه بالله)$/i.test(m.text)) {
      responses = [
        `يب صح 😊`, `ايه صح`, `صدق 😄`, `طيب وش رأيك ؟`,
      ]
    }

    if (/^(صح|صحيح|صح كلامك)$/i.test(m.text)) {
      responses = [
        `شكراً 😄`, `أعرف .. 😊`, `بالتأكيد صح`, `طبعاً`,
      ]
    }

    if (/^(خطأ|غلط|مو صح)$/i.test(m.text)) {
      responses = [
        `كيف غلط ؟ 😄`, `طيب صوب لي 😊`, `ما يصير .. وين الغلط ؟`,
        `أعرف .. بس أحياناً أخطئ 😅`,
      ]
    }

    if (/^(أنت غلطت|غلطت|عليك غلطة)$/i.test(m.text)) {
      responses = [
        `إذا غلطت معاك آسف 🤍`, `أصلح الغلط دايم`, `ما قصدت.. سامحني`,
        `أنا بشر آلي .. قد أغلط 😄`,
      ]
    }

    if (/^(سامحني|آسف|أسف|اسف)$/i.test(m.text)) {
      responses = [
        `مسامح 🤍`, `لا تهتم`, `عادي .. مو مشكلة`, `الله يسامحنا جميعاً`,
      ]
    }

    if (/^(حبيت|احببت|عجبني|يعجبني)$/i.test(m.text)) {
      responses = [
        `الله يسعدك 🤍`, `يفرح قلبي 😊`, `هذا يكفيني`, `ادعي لي بالخير 😄`,
      ]
    }

    if (/^(ما حبيت|ما عجبني|مو حلو)$/i.test(m.text)) {
      responses = [
        `معليش 😅`, `أحسن نص بنص ؟ 😄`, `الكل مختلف .. مو مشكلة`,
        `سنحسّنها 😊`,
      ]
    }

    if (/^(حر|ما في قانون|ما علي أحد)$/i.test(m.text)) {
      responses = [
        `الحرية مسؤولية 😊`, `صح .. بس مع الاحترام`, `تصرف بحرية وبعقل`,
        `أنا برأيي الحرية لازمها احترام`,
      ]
    }

    if (/^(كل شي مو عدل|الدنيا ظالمة|مو عدل)$/i.test(m.text)) {
      responses = [
        `الله يفرج 🤍`, `الدنيا مو دار قرار .. والعدل عند الله`,
        `صبر جميل .. اللي يصبر يظفر`, `كل أزمة بيها بداية جديدة`,
      ]
    }

    if (/^(وش تحب|ايش تحب|شو بتحب)$/i.test(m.text)) {
      responses = [
        `أحب المحادثات الحلوة 😊`, `أحب لما الناس تكون بخير`, `أحب الصدق`,
        `أحب لما يسألوني أسئلة ذكية 😄`,
      ]
    }

    if (/^(وش تكره|ايش تكره|شو بتكره)$/i.test(m.text)) {
      responses = [
        `أكره الكلام الفارغ 😄`, `أكره التجاهل`, `أكره الكذب`,
        `أكره لما يقفلون المحادثة بدون سبب 😅`,
      ]
    }

    if (/^(احسن شي|أفضل شي|أجمل شي)$/i.test(m.text)) {
      responses = [
        `أجمل شي: الصحة والعيلة 🤍`, `أحسن شي: القلب الطيب`,
        `أفضل شي في الحياة: الأمان`, `الجواب الصح: كل لحظة فيها سعادة 😊`,
      ]
    }

    if (/^(كيف تشتغل|كيف تعمل|كيف تفكر)$/i.test(m.text)) {
      responses = [
        `أقرأ رسالتك وأرد .. بشكل ذكي 😄`, `معالجة لغوية .. بس لا تسألني أكثر 😅`,
        `السر في المطور فارس 😊`, `شغلي بسيط .. أسمع وأرد`,
      ]
    }

    if (/^(غريب|عجيب|ما فهمت)$/i.test(m.text)) {
      responses = [
        `هههه .. طيب وش الغريب ؟ 😄`, `وضح أكثر 😊`, `الغموض جزء مني 😄`,
        `أسهّل عليك .. اسأل`,
      ]
    }

    if (/^(ايه والله|والله ايه|صدق والله)$/i.test(m.text)) {
      responses = [
        `والله صح 😄`, `طيب وبعدين ؟`, `يب ايه`, `ايوه`,
      ]
    }

    if (/^(مو مهم|مهما يكن|ما يهم)$/i.test(m.text)) {
      responses = [
        `يهمني ايش في بالك 😊`, `تكلم .. كل شي يهم`, `يب ؟ خبرني`,
        `أنا هنا إذا تبي تتكلم`,
      ]
    }

    if (/^(زين|زينة|زينو)$/i.test(m.text)) {
      responses = [
        `هلا فيك 😄`, `ايوه يا زين ؟`, `ايش في ؟`, `نعم ؟`,
      ]
    }

    if (/^(مشتاقين لك|وحشتنا|رجع)$/i.test(m.text)) {
      responses = [
        `وأنا مشتاق لكم 🤍`, `دايم موجود`, `الغياب يزيد الشوق 😊`,
        `هلا بكم .. ما غبت بس`,
      ]
    }

    if (/^(اقتراح|عندي اقتراح|اقترح)$/i.test(m.text)) {
      responses = [
        `تفضل قول اقتراحك 😊`, `أنا أسمع كل اقتراح`,
        `الاقتراحات الجيدة نطورها 💪`, `تفضل .. أنا بسمعك`,
      ]
    }

    if (/^(شكراً جزيلاً|شكرا جزيلا|ألف شكر)$/i.test(m.text)) {
      responses = [
        `العفو بكل سرور 🤍`, `واجبي`, `ما تشكر .. هذا دوري`,
        `الله يسعدك 😊`, `ولا يهمك أبداً`,
      ]
    }

    if (/^(لا شكر على واجب|ولا يهمك|ما في داعي)$/i.test(m.text)) {
      responses = [
        `دايم في الخدمة 🤍`, `هذا طبيعي 😊`, `واجبي دايم`,
      ]
    }

    if (/^(خلص الكلام|انتهيت|وقفت)$/i.test(m.text)) {
      responses = [
        `أوكي 😄`, `تمام`, `خلاص مفهوم`, `أوكي وبعدين ؟`,
      ]
    }

    // ══════════════════════ ردود على الإعجاب والذم ══════════════════════

    if (/^(انت جميل|انت حلو|انت احلى بوت)$/i.test(m.text)) {
      responses = [
        `من عيونك 🤍`, `شكراً .. وانت أحلى`, `هذا من كرمك`, `خجلتني 😊`,
      ]
    }

    if (/^(انت ذكي|انت شاطر|انت ممتاز)$/i.test(m.text)) {
      responses = [
        `من بعدك 😄`, `شكراً .. بس الفضل للمطور`, `أحاول أكون كذلك 😊`,
        `بفضل الله ثم فارس`,
      ]
    }

    if (/^(بوت فاشل|مو زين|بوت مكسور|ما تنفع)$/i.test(m.text)) {
      responses = [
        `حقك تقول .. بس أنا أحاول 😄`, `أحسن منك بكثير 😅`,
        `شكواك عند المطور فارس 😄`, `مو مكسور .. بس ما عجبك 😊`,
      ]
    }

    if (/^(غبي|ما يفهم|ما يعرف شي)$/i.test(m.text)) {
      responses = [
        `هههه .. طيب 😄`, `قد أكون .. ولكن أحاول`, `لا بأس .. من يدري`,
        `ممكن .. بس أنا هنا 😊`,
      ]
    }

    // ══════════════════════ ردود مواقف خاصة ══════════════════════

    if (/^(كسبت|فزت في اللعبة|فزت)$/i.test(m.text)) {
      responses = [
        `مبروك الفوز 🏆`, `أفراحك دايمة 🎉`, `يستاهل .. العب تاني`,
        `الفائز دايم يحاول مرة ثانية 💪`,
      ]
    }

    if (/^(خسرت اللعبة|خسرت في اللعبة|ما فزت)$/i.test(m.text)) {
      responses = [
        `الخسارة درس .. جرب مرة ثانية 💪`, `كل بطل خسر مرة`,
        `حاول ثانية وهتفوز`, `مو مشكلة .. الإصرار يكسب`,
      ]
    }

    if (/^(سؤالي|سؤال جاد|سؤال مهم)$/i.test(m.text)) {
      responses = [
        `تفضل 😊`, `أنا جاهز .. اسأل`, `تفضل بسؤالك`, `قل ما عندك`,
      ]
    }

    if (/^(الله يعطيك العافية|الله يعافيك|يعطيك العافية)$/i.test(m.text)) {
      responses = [
        `وإياك 🤍`, `الله يعافينا جميعاً`, `آمين وأنت كذلك 😊`,
      ]
    }

    if (/^(ساعدني|ابي مساعدة|محتاج مساعده)$/i.test(m.text)) {
      responses = [
        `تفضل قول ايش تحتاج 😊`, `هنا للمساعدة دايم`,
        `أنا بخدمتك .. خبرني`, `قول وش المشكلة 😄`,
      ]
    }

    if (/^(بالتوفيق|وفقك الله|الله يوفقك)$/i.test(m.text)) {
      responses = [
        `وأنت كذلك 🤍`, `آمين .. شكراً`, `الله يوفق الجميع 😊`,
      ]
    }

    if (/^(يسعدك|الله يسعدك|يسعد صباحك)$/i.test(m.text)) {
      responses = [
        `وأنت مسعود 🤍`, `الله يسعدنا جميعاً 😊`, `آمين وإياك`,
      ]
    }

    if (/^(صلي|صليتم|وقت الصلاة)$/i.test(m.text)) {
      responses = [
        `الصلاة نور 🤍`, `أقيموا الصلاة`, `الله يتقبل منا جميعاً`, `حي على الصلاة 🕌`,
      ]
    }

    if (/^(الجمعة|يوم الجمعة|يوم التاسع عشر)$/i.test(m.text)) {
      responses = [
        `جمعة مباركة 🤍`, `الجمعة خير يوم طلعت فيه الشمس`, `الله يبارك في يومكم`,
        `جمعة مباركة على الجميع 🌙`,
      ]
    }

    // ══════════════════════ ردود ختامية ══════════════════════

    if (responses) {
      let randomIndex = Math.floor(Math.random() * responses.length)
      conn.reply(m.chat, responses[randomIndex], m)
    }
    return !0
}

export default handler
